<Window x:Class="ZippyZpl.View.LabelSizeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZippyZpl.View"
        mc:Ignorable="d"
        Title="LabelSizeView"
        Height="100" Width="150"
        WindowStartupLocation="CenterOwner">
    <Grid Background="#FFC3C3C3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0">Width:</Label>
        <TextBox Name="widthTextBox" Grid.Column="1" Grid.Row="0" MinWidth="40" Text="{Binding LabelWidth}">
        </TextBox>
        <Label Grid.Column="0" Grid.Row="1">Height:</Label>
        <TextBox Name="heightTextBox" Grid.Column="1" Grid.Row="1" MinWidth="40" Text="{Binding LabelHeight}">
        </TextBox>
    </Grid >
</Window>
